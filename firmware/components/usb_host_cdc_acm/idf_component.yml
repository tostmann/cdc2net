dependencies:
  idf: '>=5.0'
  usb:
    public: true
    rules:
    - if: idf_version >=6.0
    - if: target not in ["linux"]
    version: ^1.0.0
description: USB Host CDC-ACM driver
files:
  exclude:
  - test_app
  - host_test
issues: https://github.com/espressif/esp-usb/issues
repository: git://github.com/espressif/esp-usb.git
repository_info:
  commit_sha: d2674397bc5ce0b5d9262fb179fdb06c1090801c
  path: host/class/cdc/usb_host_cdc_acm
tags:
- usb
- usb_host
- cdc
# Local removal (CDC2NET merge): the upstream `targets:` list excluded C3/C6.
# For a LOCAL component in components/ a targets-mismatch is a HARD ERROR (not a
# silent exclude), which broke the C6 build at the manifest phase.  Exclusion is
# done instead by the CMakeLists OTG guard (registers empty on non-OTG SoCs).
# Do NOT re-add `targets:` here.  See docs/merge-targets.md §3.2.
url: https://github.com/espressif/esp-usb/tree/master/host/class/cdc/usb_host_cdc_acm
version: 2.4.0
